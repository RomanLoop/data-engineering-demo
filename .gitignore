# --- Secrets — never commit these (see AGENT.md §8) ---
.env
.env.*
!.env.example
*.pem
*.key

# Claude Code local/machine-specific state (not the shared skills/commands)
.claude/settings.local.json
.claude/*.local.*

# --- dbt ---
target/
dbt_packages/
logs/
.user.yml

# --- Meltano ---
.meltano/
output/

# --- Stage-1 landing source CSVs (reproducible download, see
#     data/contoso/README.md) — kept out of git: orderrows.csv is ~86 MB,
#     orders.csv ~46 MB, and all six are byte-identical to a public
#     release asset. ---
data/contoso/*.csv

# --- Dagster ---
.dagster_home/
storage/
orchestration/.dg/
orchestration/.tmp_dagster_home_*/

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# --- OS ---
.DS_Store
Thumbs.db
