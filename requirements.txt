# Direct dependencies for the local dev/analytics environment.
# Create the venv IN THE PROJECT (never install globally):
#
#   python -m venv .venv
#   .venv\Scripts\python -m pip install -U pip
#   .venv\Scripts\pip install -r requirements.txt
#
# `requirements.lock.txt` is the full `pip freeze` of a known-good env —
# use it (`pip install -r requirements.lock.txt`) for an exact reproduce.
#
# Windows note: this repo lives under a long path. `.venv` inside it is
# still shorter than a global/Store-Python site-packages path, but if pip
# hits a MAX_PATH error, enable long paths (admin):
#   Set-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem' LongPathsEnabled 1
# `git config core.longpaths true` is already set for this repo.

# --- transformation ---
# dbt-core pinned to 1.11.x (not the latest 1.12.x) because dagster-dbt==0.29.21 (the latest
# release as of 2026-09-11) caps at dbt-core<1.12 — every published dagster-dbt version does,
# checked back through 0.27.x. dbt-fabricspark only needs dbt-core>=1.8.0, so this is a real
# downgrade made to satisfy the Dagster integration (specs/003-dagster-orchestration), not an
# adapter requirement. Re-validated (2026-09-11): dbt parse/compile/build + the full 001/002 test
# suite pass unchanged at 1.11.15 — see specs/003-dagster-orchestration/research.md Decision 10.
dbt-core==1.11.15
dbt-fabricspark==1.13.4

# --- linting ---
sqlfluff==4.3.0
sqlfluff-templater-dbt==4.3.0

# --- task runner (poe) ---
poethepoet==0.48.0

# --- helper scripts (upload_to_onelake.py, delta-load simulators, onboarding) ---
azure-identity==1.25.3
requests==2.34.2
py7zr==1.1.3

# --- orchestration (Dagster, specs/003-dagster-orchestration) ---
# create-dagster/dagster-dg-cli are only needed for scaffolding/dg CLI use; kept pinned for
# reproducibility. Installed via pip into this .venv — NOT uv (research.md Decision 1: this repo
# standardized on a single pip-managed .venv, declining the scaffold tool's uv default).
dagster==1.13.21
dagster-webserver==1.13.21
dagster-dbt==0.29.21
dagster-dg-cli==1.13.21
create-dagster==1.13.21
